# 📋 設定頁面 UI 更新

> 文件編號: 20260112-001  
> 更新日期: 2026-01-12  
> 類型: 新功能

---

## 📦 更新內容

新增用戶設定頁面 UI，包含：
- 快速模板（極簡/標準/完整/短線）
- 指標顯示開關（7 種技術指標）
- 通知設定開關（8 種 LINE 推播警報）
- 進階參數調整（14 種可自訂參數）

---

## 📁 新增檔案

| 檔案 | 位置 | 說明 |
|------|------|------|
| `settings.css` | `static/css/` | 設定頁面樣式 |
| `settings.js` | `static/js/` | 設定頁面邏輯 |
| `settings-section.html` | `static/` | HTML 片段 |

---

## 🚀 整合步驟

### 1. 複製檔案

```bash
cp static/css/settings.css /你的專案/static/css/
cp static/js/settings.js /你的專案/static/js/
```

### 2. 修改 dashboard.html

#### 在 `<head>` 加入：
```html
<link rel="stylesheet" href="/static/css/settings.css">
```

#### 在 `</body>` 前加入：
```html
<script src="/static/js/settings.js"></script>
```

#### 加入 HTML section：
將 `settings-section.html` 內容貼到其他 section 之後

#### 加入導航連結：

**桌面版：**
```html
<a onclick="showSection('settings', event)" class="nav-link ...">
    <i class="fas fa-cog mr-2"></i>設定
</a>
```

**手機版：**
```html
<button onclick="showSection('settings', event)" class="nav-tab ...">
    <i class="fas fa-cog text-lg"></i>
    <span class="text-xs mt-1">設定</span>
</button>
```

### 3. 修改 showSection 函數

```javascript
if (name === 'settings') {
    if (typeof initSettingsPage === 'function') {
        initSettingsPage();
    }
}
```

---

## 🔧 API 依賴

| 端點 | 方法 | 說明 |
|------|------|------|
| `/api/settings/indicators` | GET/PUT | 指標顯示設定 |
| `/api/settings/alerts` | GET/PUT | 通知設定 |
| `/api/settings/params` | GET/PUT | 參數設定 |
| `/api/settings/template/{name}` | POST | 套用模板 |

---

## ✅ 驗證

1. 可切換到設定頁面
2. 設定正常載入
3. 設定可儲存
4. 模板可套用
5. 手機版顯示正常
